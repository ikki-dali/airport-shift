# TICKET-044: 【Mobile】募集一覧・エントリー画面

## 担当
Frontend

## 概要
人手が足りないシフトの募集を表示し、バイトがエントリーできる画面を作成する。
**第3優先機能** - 不足シフトを埋める重要な機能。

## リポジトリ
`ikki-dali/airport-shift-mobile`

## 画面要件

### 募集タブ（app/(tabs)/recruit.tsx）

#### ヘッダー
- 「募集中のシフト」タイトル
- 件数バッジ（例: 3件）

#### 募集リスト
人手が足りないシフトをカード形式で表示：

```
┌─────────────────────────────┐
│ 🔴 急募                      │
│ 2/10（土）09:00-18:00       │
│ 第1ターミナル               │
│ あと2名必要                 │
│                             │
│ [エントリーする]            │
└─────────────────────────────┘
```

各カードに表示：
- 日付・曜日
- 時間帯
- 勤務地
- 必要人数（あと○名）
- 急募マーク（直近の場合）

#### エントリー機能
- 「エントリーする」ボタンタップ
- 確認ダイアログ：「2/10のシフトにエントリーしますか？」
- 成功時：トースト表示、リストから消えるor「エントリー済み」表示

### データ取得
```typescript
// 人手不足のシフトを取得
// shifts と location_requirements を比較して不足を検出
const { data: shifts } = await supabase
  .from('shifts')
  .select(`*, location:locations(*), duty_code:duty_codes(*)`)
  .gte('date', today)
  .order('date')

// 各日・各勤務地の必要人数と実際の人数を比較
// 不足があれば募集リストに追加
```

### エントリー保存
```typescript
// shift_entriesテーブル（なければ作成）
const { error } = await supabase
  .from('shift_entries')
  .insert({
    staff_id: staffId,
    date: '2024-02-10',
    location_id: locationId,
    status: 'pending' // pending -> approved/rejected
  })
```

## UI/UX
- 募集が目立つデザイン（赤やオレンジ）
- 急募は特に目立たせる（3日以内など）
- エントリーはワンタップで
- エントリー済みは視覚的にわかりやすく

## 完了条件
- [ ] 募集中のシフト一覧が表示される
- [ ] 日付、時間帯、勤務地、必要人数が表示される
- [ ] エントリーボタンでエントリーできる
- [ ] エントリー済みの表示が変わる
- [ ] 急募（直近）が目立つ
- [ ] スマホで見やすいUI

## 備考
- 認証は未実装（ダミーのstaff_idでOK）
- shift_entriesテーブルがなければ作成（または既存テーブルを流用）
- プッシュ通知は今回のスコープ外（後で追加）

## DBテーブル案（必要なら）
```sql
create table shift_entries (
  id uuid primary key default gen_random_uuid(),
  staff_id uuid references staff(id),
  date date not null,
  location_id uuid references locations(id),
  status text default 'pending', -- pending, approved, rejected
  created_at timestamptz default now()
);
```
